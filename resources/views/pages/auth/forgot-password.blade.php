@extends('layouts.app')
@section('title', '找回密码 · 阅境')
@section('content')<main class="auth-page"><section class="auth-panel"><p class="eyebrow">ACCOUNT RECOVERY</p><h1>找回你的密码</h1><p>输入注册邮箱，我们会把重置链接发给你。</p>@if (session('status'))<div class="alert">{{ session('status') }}</div>@endif<form class="form-stack" method="POST" action="{{ Route::has('password.email') ? route('password.email') : '#' }}">@csrf<div class="form-field"><label for="email">注册邮箱</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="email"></div><button class="button button-primary form-submit" type="submit">发送重置链接 <span>→</span></button></form><p class="auth-foot"><a href="{{ Route::has('login') ? route('login') : '#' }}">返回登录</a></p></section></main>@endsection