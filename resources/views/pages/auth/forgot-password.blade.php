@extends('layouts.app')
@section('title', __('ui.auth.forgot_title'))
@section('content')<main class="auth-page"><section class="auth-panel"><p class="eyebrow">{{ __('ui.auth.forgot_eyebrow') }}</p><h1>{{ __('ui.auth.forgot_heading') }}</h1><p>{{ __('ui.auth.forgot_intro') }}</p>@if (session('status'))<div class="alert">{{ session('status') }}</div>@endif<form class="form-stack" method="POST" action="{{ Route::has('password.email') ? route('password.email') : '#' }}">@csrf<div class="form-field"><label for="email">{{ __('ui.auth.registered_email') }}</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="email"></div><button class="button button-primary form-submit" type="submit">{{ __('ui.auth.send_reset') }} <span>→</span></button></form><p class="auth-foot"><a href="{{ Route::has('login') ? route('login') : '#' }}">{{ __('ui.auth.back_login') }}</a></p></section></main>@endsection
