@extends('layouts.app')
@section('title', __('ui.auth.login_title'))
@section('content')<main class="auth-page"><section class="auth-panel"><p class="eyebrow">{{ __('ui.auth.login_eyebrow') }}</p><h1>{{ __('ui.auth.login_heading') }}</h1><p>{{ __('ui.auth.login_intro') }}</p>@if ($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form class="form-stack" method="POST" action="{{ Route::has('login') ? route('login') : '#' }}">@csrf<div class="form-field"><label for="email">{{ __('ui.auth.email') }}</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="email"></div><div class="form-field"><label for="password">{{ __('ui.auth.password') }}</label><input id="password" name="password" type="password" required autocomplete="current-password"></div><div class="form-options"><label><input type="checkbox" name="remember"> {{ __('ui.auth.remember') }}</label><a href="{{ Route::has('password.request') ? route('password.request') : '#' }}">{{ __('ui.auth.forgot') }}</a></div><button class="button button-primary form-submit" type="submit">{{ __('ui.auth.login_submit') }} <span>→</span></button></form><p class="auth-foot">{{ __('ui.auth.no_account') }} <a href="{{ Route::has('register') ? route('register') : '#' }}">{{ __('ui.auth.register_link') }}</a></p></section></main>@endsection
