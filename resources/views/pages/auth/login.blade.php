@extends('layouts.app')
@section('title', '登录 · 阅境')
@section('content')<main class="auth-page"><section class="auth-panel"><p class="eyebrow">WELCOME BACK</p><h1>回到你的阅境</h1><p>继续读完那些还没读完的故事。</p>@if ($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form class="form-stack" method="POST" action="{{ Route::has('login') ? route('login') : '#' }}">@csrf<div class="form-field"><label for="email">邮箱</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="email"></div><div class="form-field"><label for="password">密码</label><input id="password" name="password" type="password" required autocomplete="current-password"></div><div class="form-options"><label><input type="checkbox" name="remember"> 记住我</label><a href="{{ Route::has('password.request') ? route('password.request') : '#' }}">忘记密码？</a></div><button class="button button-primary form-submit" type="submit">登录阅境 <span>→</span></button></form><p class="auth-foot">还没有账号？ <a href="{{ Route::has('register') ? route('register') : '#' }}">注册一个新账号</a></p></section></main>@endsection