@extends('layouts.app')
@section('title', '注册 · 阅境')
@section('content')<main class="auth-page"><section class="auth-panel"><p class="eyebrow">JOIN YUEJING</p><h1>从一页开始，写下你的阅境</h1><p>注册后收藏作品、同步进度，也可以发布属于你的故事。</p>@if ($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<form class="form-stack" method="POST" action="{{ Route::has('register') ? route('register') : '#' }}">@csrf<div class="form-field"><label for="name">昵称</label><input id="name" name="name" type="text" value="{{ old('name') }}" required autocomplete="name"></div><div class="form-field"><label for="email">邮箱</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="email"></div><div class="form-field"><label for="password">设置密码</label><input id="password" name="password" type="password" required autocomplete="new-password"></div><div class="form-field"><label for="password_confirmation">确认密码</label><input id="password_confirmation" name="password_confirmation" type="password" required autocomplete="new-password"></div><button class="button button-primary form-submit" type="submit">创建账号 <span>→</span></button></form><p class="auth-foot">已经有账号？ <a href="{{ Route::has('login') ? route('login') : '#' }}">直接登录</a></p></section></main>@endsection